<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sorezel.burritos">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--<permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" android:protectionLevel="signature"/>-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ConfiguracionActivity"
            android:label="@string/title_activity_configuracion"
            android:parentActivityName=".UsuarioActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sorezel.burritos.UsuarioActivity" />
        </activity>
        <activity
            android:name=".prueba_scroll"
            android:label="@string/title_activity_prueba_scroll"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ejemplo_drawer"
            android:label="@string/title_activity_ejemplo_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MoinActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetalleActivity" />
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".DetalleOrdenActivity" />
    </application>

</manifest>